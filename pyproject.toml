[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nexusai-backend"
version = "1.0.0"
description = "NexusAI - AI Assistant Backend"
requires-python = ">=3.11"
